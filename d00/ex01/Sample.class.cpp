int main( void )
{
	Contact		listContact[8];
	std::string	cmd;

	while (77) {
		std::cout << "choose a new-command[ADD - SEARCH - EXIT]" << std::endl;
		if (std::cin == "Exit")
				{
					std::cout << "ah tu veux m'quitter" << std::endl;
					return (0);
				}
		else if (std::cin == "ADD")


	}
}
